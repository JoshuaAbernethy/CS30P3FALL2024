package additionalExamples;

import java.util.Scanner;

public class Zelmark {
	
	double pay;
	
	system.out.println(emo);
	String response;
	pay = emp.pay(payArg);
	System.out.println(money.format(pay));
	
	public static void main(String[] args)
	{//
		Manager emp 1 = new Manager("Yoki, ");
		Associate emp2 = new 
				
				Scanner input = new Scanner(System.in)
				
				String actin\on;
		int empNum;
		double payArg;
		
		Employee emp = emp1
				
		do
		{
			System.out.println("/N (E)mployee // (P)ay // (Q)uit");
			System.out.println("Enter choice: ");
			action = input.next();
			
			if(!action.equalsIgnoreCase("Q"))
			{
				System.out.println("Enter Employee number(1 or 2)");
				empNum = input.nextInt();
				
				switch(empNum)
				{
				case 1: emp = emp1; break;
				case 2: emp = emp2; breal;
				}
			}
			
			if(action.equalsIgnoreCase("E"))
			{
				System.out.println(emp);
			}
			else if(action.equalsIgnorecase("P"))
			{
				System.out.println("Enter number of hours worked: " + "for Associate OR pay for " + "Manager");
				payArg = input.nextDouble();
				payEmployee(emp, payArg);
			}
			
		}
	}while(!action.equalsIgnoreCase("Q"));
	}

}
